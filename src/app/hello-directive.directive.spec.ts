import { HelloDirectiveDirective } from './hello-directive.directive';

describe('HelloDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HelloDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
